<Window
    x:Class="ImageRecognitionSurfUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ImageRecognitionSurfUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:ImageRecognitionSurfUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ImageRecognitionSurfUI.ViewModels"
    Title="{Binding Title}"
    Width="800"
    Height="285"
    d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel,
                                     IsDesignTimeCreatable=False}"
    d:Height="400"
    d:Width="600"
    mc:Ignorable="d">
    <Window.Resources>
        <Style x:Key="GroupTitleTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Margin" Value="4,4,4,8" />
            <Setter Property="Foreground" Value="Navy" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button
            Grid.Row="0"
            Grid.Column="0"
            Margin="2"
            Padding="4"
            VerticalAlignment="Top"
            Command="{Binding LoadSourceImageCommand}"
            Content="Загрузить изображение" />

        <Grid
            Grid.Row="0"
            Grid.RowSpan="2"
            Grid.Column="3"
            Margin="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Expander
                Grid.Row="1"
                DataContext="{Binding ProcessOptionsViewModel}"
                Header="Опции"
                IsExpanded="True">
                <StackPanel Orientation="Vertical">
                    <TextBlock Margin="2" Text="Test icon file" />
                    <ComboBox
                        Margin="2"
                        DisplayMemberPath="Name"
                        IsTextSearchCaseSensitive="False"
                        IsTextSearchEnabled="True"
                        ItemsSource="{Binding IconFiles}"
                        SelectedValue="{Binding SelectedIconFile, UpdateSourceTrigger=PropertyChanged}" />

                    <GroupBox Header="Общие настройки" Padding="4" Margin="2">
                        <UniformGrid Columns="2">
                            <TextBlock Margin="2" Text="Convert to gray options" />
                            <CheckBox IsChecked="{Binding UseConvertColorsOptions}"  />

                            <TextBlock Margin="2" Text="SURF HessianThreshold" />
                            <TextBox Text="{Binding SurftRecognizer_HessianThreshold}"  />
                        </UniformGrid>
                    </GroupBox>
                    
                    <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding UseCannyOptions}" VerticalAlignment="Center"  />
                        <TextBlock Style="{StaticResource GroupTitleTextBlockStyle}" Text="Canny options" />
                    </StackPanel>


                    <UniformGrid Columns="2">
                        <TextBlock Margin="2" Text="Canny Threshold1" />
                        <TextBox Margin="2" Text="{Binding Canny_Treshold1, UpdateSourceTrigger=LostFocus}" />

                        <TextBlock Margin="2" Text="Canny Threshold2" />
                        <TextBox Margin="2" Text="{Binding Canny_Treshold2, UpdateSourceTrigger=LostFocus}" />

                        <TextBlock Margin="2" Text="Canny Apperture Size" />
                        <TextBox Margin="2" Text="{Binding Canny_AppertureSize, UpdateSourceTrigger=LostFocus}" />

                        <TextBlock Margin="2" Text="Canny L2 Gradient" />
                        <CheckBox Margin="2" IsChecked="{Binding Canny_L2Gradient, UpdateSourceTrigger=PropertyChanged}" />
                    </UniformGrid>
                    <UniformGrid Columns="2" />
                    <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding UseThresholdOptions}" VerticalAlignment="Center"  />
                        <TextBlock Style="{StaticResource GroupTitleTextBlockStyle}" Text="Threshold options" />
                    </StackPanel>
                    <UniformGrid Columns="2">
                        <TextBlock Margin="2" Text="Threshold Thresh" />
                        <TextBox Margin="2" Text="{Binding Threshold_Thresh, UpdateSourceTrigger=LostFocus}" />

                        <TextBlock Margin="2" Text="Threshold MaxVal" />
                        <TextBox Margin="2" Text="{Binding Threshold_MaxVal, UpdateSourceTrigger=LostFocus}" />

                        <TextBlock Margin="2" Text="Threshold Type" />
                        <ComboBox
                            Margin="2"
                            ItemsSource="{Binding Threshold_Types, Mode=OneWay}"
                            SelectedValue="{Binding Threshold_Type, UpdateSourceTrigger=PropertyChanged}" />

                    </UniformGrid>
                </StackPanel>
            </Expander>
        </Grid>

        <Button
            Grid.Row="0"
            Grid.Column="2"
            Margin="2"
            Padding="4"
            VerticalAlignment="Top"
            Command="{Binding RotateAgnosticCheckCommand}"
            Content="Перерисовать" />
        <controls:ImageSourceControl
            Grid.Row="1"
            Grid.Column="0"
            CurrentImagePath="{Binding SourceImagePath}" />
        <GridSplitter
            Grid.Row="0"
            Grid.RowSpan="3"
            Grid.Column="1"
            Width="3"
            HorizontalAlignment="Center"
            VerticalAlignment="Stretch"
            Background="DarkGray"
            ShowsPreview="False" />


        <controls:ImageSourceControl
            Grid.Row="1"
            Grid.Column="2"
            CurrentImagePath="{Binding ResultImagePath}" />


    </Grid>
</Window>
